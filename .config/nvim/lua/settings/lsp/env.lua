local M = {}

function M.set_env()
    -- local current_dir = vim.fn.getcwd()
    -- JDTLS_CMD = ""
    -- JAVA_HOME = ""
    -- if string.match(current_dir, "^/path/to/dir") then
    --     JAVA_HOME = "/path/to/java/home"
    --     JDTLS_CMD = "/path/to/bin/jdtls"
    -- else
    --     JAVA_HOME = "/path/to/java/home"
    --     JDTLS_CMD = "/path/to/bin/jdtls"
    -- -- end

    -- vim.env.JAVA_HOME = JAVA_HOME
    -- vim.env.PATH = JAVA_HOME .. "/bin:" .. vim.env.PATH
end

return M
